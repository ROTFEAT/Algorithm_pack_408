#pragma once
#include<iostream>
#include<vector>
using namespace std;

template <typename T>
T FindBackN(vector<T> ve, int n)
{
	auto it = ve.begin();
	auto it2 = it + n;
	while (it2 != ve.end())
	{
		
	}
	return 
}
